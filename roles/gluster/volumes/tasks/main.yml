---
- include_tasks: volume.yml
  vars:
    gluster_volume_config: "{{ item }}"
  with_items: "{{ gluster_volumes }}"

- include_tasks: mount.yml
  vars:
    gluster_volume_config: "{{ item }}"
  with_items: "{{ gluster_volumes }}"
  # Only where current node hosts data !!!