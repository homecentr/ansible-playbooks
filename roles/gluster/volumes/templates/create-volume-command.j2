gluster volume create {{ gluster_volume_config.name }} replica {{ gluster_volume_config.replicas }} arbiter {{ gluster_volume_config.arbiters }} {% for storage_brick in gluster_volume_config.storage_bricks -%} {{ storage_brick.host }}:{{ storage_brick.path }} {% endfor %} {% for arbiter_brick in gluster_volume_config.arbiter_bricks -%} {{ arbiter_brick.host }}:{{ arbiter_brick.path }} {%- endfor %} force