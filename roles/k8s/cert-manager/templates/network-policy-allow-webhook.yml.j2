apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cert-manager-allow-inbound-webhook
  namespace: cert-manager
spec:
  podSelector: 
    matchLabels:
      app: webhook
  ingress:
    - from: # Allow calls from Kube API
{% for node in groups['k8s_nodes'] %}
        - ipBlock:
            cidr: {{ hostvars[node].ansible_host }}/32
{% endfor %}
        #- ipBlock:
        #    cidr: 172.16.0.0/32 # Allow from proxy network
        #- namespaceSelector:
        #    matchLabels:
        #      kubernetes.io/metadata.name: cert-manager
        #  podSelector: {}
  #egress:
  #- to:
  #  - ipBlock:
  #      cidr: 0.0.0.0/0
  policyTypes:
  - Ingress
  #- Egress