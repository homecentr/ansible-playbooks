apiVersion: v1
kind: Service
metadata:
  name: frigate
  namespace: "{{ k3s_namespace | mandatory }}"
  labels:
    app.kubernetes.io/name: frigate
    app.kubernetes.io/managed-by: ansible
spec:
  ports:
  - name: webui
    port: 5000
  - name: rtsp
    port: 1935
  selector:
    app.kubernetes.io/name: frigate