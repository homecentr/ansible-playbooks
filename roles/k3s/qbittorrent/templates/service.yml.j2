apiVersion: v1
kind: Service
metadata:
  name: qbittorrent
  namespace: "{{ qbittorrent_target_namespace | mandatory }}"
spec:
  ports:
  - name: "web-ui"
    port: 8080
  selector:
    app: qbittorrent