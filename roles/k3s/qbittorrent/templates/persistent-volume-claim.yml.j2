kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: qbittorrent-config
  namespace: "{{ qbittorrent_target_namespace | mandatory }}"
spec:
  storageClassName: {{ qbittorrent_storage_class | mandatory }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi