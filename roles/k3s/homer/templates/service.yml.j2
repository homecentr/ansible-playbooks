apiVersion: v1
kind: Service
metadata:
  name: homer
  namespace: "{{ k3s_namespace | mandatory }}"
  labels:
    app.kubernetes.io/name: homer
    app.kubernetes.io/managed-by: ansible
spec:
  ports:
  - name: http
    port: 8080
  selector:
    app.kubernetes.io/name: homer