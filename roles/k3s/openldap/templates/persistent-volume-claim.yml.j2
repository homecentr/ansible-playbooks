kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: openldap
  namespace: "{{ openldap_target_namespace | mandatory }}"
spec:
  storageClassName: {{ openldap_storage_class | mandatory }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ openldap_storage_size }}