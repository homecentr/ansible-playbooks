apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: services-pool
      protocol: layer2
      addresses:
      - "{{ k3s_services_vips_cidr | mandatory }}"