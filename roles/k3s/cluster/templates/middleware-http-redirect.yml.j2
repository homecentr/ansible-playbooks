apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-https
  namespace: "{{ k3s_namespace | mandatory }}"
spec:
  redirectScheme:
    scheme: https
    permanent: true