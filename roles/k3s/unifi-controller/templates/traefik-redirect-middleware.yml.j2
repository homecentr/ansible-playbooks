apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: unifi-controller-redirect
  namespace: "{{ unifi_controller_target_namespace | mandatory }}"
spec:
  redirectScheme:
    scheme: https
    permanent: true