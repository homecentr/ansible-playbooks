---
- import_tasks: ../../shared/docker-stack-iam.yml
- import_tasks: ../../shared/docker-socket-iam.yml

- name: Traefik stack
  run_once: true
  docker_stack:
    prune: yes
    name: homecentr-traefik
    compose:
      - "{{ lookup('template', 'docker-compose.yml.j2') | from_yaml }}"